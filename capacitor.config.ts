import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.dimetu.appMovilNew',
  appName: 'app-movil-new',
  webDir: 'dist/app-movil-new/browser', // Asegúrate de que esta ruta sea correcta
};
export default config;
